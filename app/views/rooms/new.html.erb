<h1>Rooms#new</h1>
<%= form_with( model: @form, local:true) do |f|%>
  <%= f.label :tag %>
  <%= f.select :tag, @tags%>

  <%= f.label :num_users %>
  <%= f.number_field :num_users%>
  <%= f.submit%>
<% end %>
